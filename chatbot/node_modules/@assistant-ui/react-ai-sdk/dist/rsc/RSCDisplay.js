"use client";

// src/rsc/RSCDisplay.tsx
import {
  useThread,
  useMessage,
  getExternalStoreMessage
} from "@assistant-ui/react";
import {
  symbolInternalRSCExtras
} from "./utils/RSCThreadExtras.js";
var RSCDisplay = () => {
  const convertFn = useThread((t) => {
    const extras = t.extras?.[symbolInternalRSCExtras];
    if (!extras)
      throw new Error(
        "This function can only be used inside a Vercel RSC runtime."
      );
    return extras.convertFn;
  });
  return useMessage((m) => convertFn(getExternalStoreMessage(m)).display);
};
export {
  RSCDisplay
};
//# sourceMappingURL=RSCDisplay.js.map